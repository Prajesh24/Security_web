# Secrets consumed by docker-compose.yml (copy to `.env` and fill in).
# Generate strong values, e.g.:  openssl rand -hex 32
JWT_SECRET=replace_with_a_long_random_secret
ENCRYPTION_KEY=replace_with_a_long_random_key
